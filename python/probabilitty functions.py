import numpy as np
import pandas as pd
import matplotlib as plt
import scipy.stats as stats
POSSION = (LAMBDA) ^ K*E ^ (-LAMBDA)/K!
UNIFOR_DENSITY = 1/(B-A)
NORMAL = 1/(SIGMA(UNDERROOT(2*PI))) * E ^ (-((X-U) ^ 2)/(2*(SIGMA**2)))
GAMMA = (1/((BETA ^ ALPHA)(A-1)!))*X ^ (ALPHA-1)*E(-X/BETA)       X > 0
EXPONENTIAL = LAMBDA*E ^ (-(LAMBDA*X))
